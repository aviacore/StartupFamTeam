# Session Notes — 2026-05-25

## What We Did

### Server Setup (Port 8765)
- Discovered port 8080 occupied by Hikvision iVMS-4200 software returning `{"code":"1-11","msg":"Invalid url."}`
- Switched all services to port 8765

### CraftStack Architecture
- Replaced Paperclip (too heavy, excessive context overhead)
- Removed Ollama (too slow, models too weak for CPU-only)
- Chose DeepSeek V4 Flash as primary provider (free, strong coding)
- Built server.py with Python stdlib only (ThreadingHTTPServer, SSE, REST API)
- Built dashboard.html with 3-column kanban + provider cards
- Built monitor.html with SSE live updates

### Windows-Specific Issues
- pythonw.exe requires full path via `%LOCALAPPDATA%\Programs\Python\Python314\`
- Print with emoji (🚀) crashes on cp1251 console — fixed by redirecting to pythonw.exe (no console)
- start /min in batch files needs full path for both python and server.py
- Old python.exe processes hold port 8765 after crashes — start.bat kills all

### 15-Puzzle Game
- Created `project/src/index.html` — 15-puzzle with A* solver, dark theme, glassmorphism
- Created `project/src/sortpuzzle.html` — alternative version: light theme, 3×3/4×4/5×5, timer

### Pipeline Workflow
- Dashboard POST /api/task → server writes task.txt + state.json
- Assistant reads task.txt when user says "start"
- Assistant updates state.json during execution
- Server pushes SSE updates to monitor
- Dashboard polls /api/state and syncs kanban columns

## Unresolved

- Auto-detection of new tasks: Assistant must be told "start" — no automatic pickup
- File:// mode for monitor works but requires refresh (SSE only when server running)
- Multiple providers not implemented (one per session)
- Remote deployment to test3.ksn.by not yet configured

## User Preferences

- Russian language, informal "ты"
- CEO/decision-maker role, not agent
- No budget, geo-blocked (Russia)
- Prefers simple file-based approaches over heavy tools
- Wants to discuss/plan before building
- Hates copy-paste workflows, wants integrated system
