# PM — Output

# Разработка WEB-приложения для управления провайдерами

## Итоговый результат

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Provider Manager</title>
    <!-- Современный дизайн с glassmorphism -->
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
            min-height: 100vh;
            background: linear-gradient(135deg, #0f0c29, #302b63, #24243e);
            display: flex;
            justify-content: center;
            align-items: flex-start;
            padding: 2rem;
            color: #e0e0e0;
        }

        .container {
            width: 100%;
            max-width: 1200px;
            backdrop-filter: blur(20px);
            background: rgba(255, 255, 255, 0.05);
            border-radius: 24px;
            padding: 2rem;
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.5);
        }

        h1 {
            font-size: 2.2rem;
            font-weight: 600;
            background: linear-gradient(90deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 0.5rem;
        }

        .subtitle {
            color: #9ca3af;
            margin-bottom: 2rem;
            font-size: 0.95rem;
        }

        .controls {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
            margin-bottom: 2rem;
        }

        .search-input {
            flex: 1;
            min-width: 200px;
            padding: 0.75rem 1.2rem;
            border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.15);
            background: rgba(255, 255, 255, 0.08);
            color: #fff;
            font-size: 1rem;
            transition: all 0.3s;
        }

        .search-input::placeholder {
            color: #6b7280;
        }

        .search-input:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 20px rgba(102, 126, 234, 0.3);
        }

        .btn-primary {
            padding: 0.75rem 1.5rem;
            border-radius: 12px;
            border: none;
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            white-space: nowrap;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
        }

        .btn-danger {
            padding: 0.5rem 1rem;
            border-radius: 8px;
            border: 1px solid rgba(239, 68, 68, 0.3);
            background: rgba(239, 68, 68, 0.1);
            color: #fca5a5;
            cursor: pointer;
            transition: all 0.3s;
            font-size: 0.85rem;
        }

        .btn-danger:hover {
            background: rgba(239, 68, 68, 0.2);
            border-color: #ef4444;
        }

        .provider-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 1.5rem;
        }

        .provider-card {
            background: rgba(255, 255, 255, 0.07);
            border-radius: 16px;
            padding: 1.5rem;
            border: 1px solid rgba(255, 255, 255, 0.08);
            transition: all 0.3s;
            backdrop-filter: blur(10px);
        }

        .provider-card:hover {
            transform: translateY(-4px);
            border-color: rgba(102, 126, 234, 0.4);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
        }

        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 1rem;
        }

        .provider-name {
            font-size: 1.2rem;
            font-weight: 600;
            color: #fff;
        }

        .provider-model {
            font-size: 0.85rem;
            color: #a5b4fc;
            background: rgba(102, 126, 234, 0.15);
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            display: inline-block;
            margin-top: 0.25rem;
        }

        .card-body {
            display: flex;
            flex-direction: column;
            gap: 0.75rem;
        }

        .field-group {
            display: flex;
            flex-direction: column;
            gap: 0.25rem;
        }

        .field-label {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            color: #6b7280;
        }

        .field-value {
            font-size: 0.95rem;
            word-break: break-all;
        }

        .field-value a {
            color: #93c5fd;
            text-decoration: none;
        }

        .field-value a:hover {
            text-decoration: underline;
        }

        .editable-field {
            display: flex;
            gap: 0.5rem;
            align-items: center;
        }

        .editable-field input {
            flex: 1;
            padding: 0.5rem 0.75rem;
            border-radius: 8px;
            border: 1px solid rgba(255, 255, 255, 0.12);
            background: rgba(255, 255, 255, 0.06);
            color: #fff;
            font-size: 0.9rem;
        }

        .editable-field input:focus {
            outline: none;
            border-color: #667eea;
        }

        .edit-btn {
            padding: 0.4rem 0.8rem;
            border-radius: 6px;
            border: 1px solid rgba(255, 255, 255, 0.12);
            background: rgba(255, 255, 255, 0.08);
            color: #9ca3af;
            cursor: pointer;
            transition: all 0.3s;
            font-size: 0.8rem;
        }

        .edit-btn:hover {
            background: rgba(255, 255, 255, 0.15);
            color: #fff;
        }

        .balance-display {
            font-weight: 600;
            color: #34d399;
        }

        .balance-display.low {
            color: #fbbf24;
        }

        .balance-display.empty {
            color: #f87171;
        }

        .modal-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.7);
            backdrop-filter: blur(8px);
            justify-content: center;
            align-items: center;
            z-index: 1000;
        }

        .modal-overlay.active {
            display: flex;
        }

        .modal {
            background: #1e1b4b;
            border-radius: 20px;
            padding: 2rem;
            max-width: 500px;
            width: 90%;
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 30px 60px rgba(0, 0, 0, 0.5);
        }

        .modal h2 {
            margin-bottom: 1.5rem;
            color: #fff;
        }

        .modal input,
        .modal select {
            width: 100%;
            padding: 0.75rem 1rem;
            margin-bottom: 1rem;
            border-radius: 10px;
            border: 1px solid rgba(255, 255, 255, 0.12);
            background: rgba(255, 255, 255, 0.06);
            color: #fff;
            font-size: 0.95rem;
        }

        .modal select option {
            background: #1e1b4b;
            color: #fff;
        }

        .modal-actions {
            display: flex;
            gap: 1rem;
            justify-content: flex-end;
            margin-top: 1rem;
        }

        .btn-secondary {
            padding: 0.6rem 1.2rem;
            border-radius: 10px;
            border: 1px solid rgba(255, 255, 255, 0.12);
            background: transparent;
            color: #9ca3af;
            cursor: pointer;
            transition: all 0.3s;
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.08);
            color: #fff;
        }

        .empty-state {
            text-align: center;
            padding: 3rem;
            color: #6b7280;
            grid-column: 1 / -1;
        }

        .empty-state svg {
            width: 80px;
            height: 80px;
            margin-bottom: 1rem;
            opacity: 0.5;
        }

        @media (max-width: 640px) {
            .provider-grid {
                grid-template-columns: 1fr;
            }
            .container {
                padding: 1rem;
            }
        }

        .toast {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            background: rgba(16, 185, 129, 0.9);
            color: white;
            padding: 1rem 1.5rem;
            border-radius: 12px;
            backdrop-filter: blur(10px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            transform: translateY(100px);
            opacity: 0;
            transition: all 0.4s;
        }

        .toast.show {
            transform: translateY(0);
            opacity: 1;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🤖 AI Provider Manager</h1>
        <p class="subtitle">Управляйте API-ключами и балансами AI-провайдеров</p>

        <div class="controls">
            <input type="text" id="searchInput" class="search-input" placeholder="🔍 Поиск провайдера или модели...">
            <button class="btn-primary" onclick="openAddModal()">+ Добавить провайдера</button>
        </div>

        <div id="providerGrid" class="provider-grid">
            <!-- Карточки будут добавлены JavaScript -->
        </div>
    </div>

    <!-- Модальное окно добавления -->
    <div id="addModal" class="modal-overlay">
        <div class="modal">
            <h2>➕ Новый провайдер</h2>
            <input type="text" id="providerName" placeholder="Название провайдера (например, OpenAI)">
            <input type="text" id="modelName" placeholder="Модель (например, GPT-4)">
            <input type="url" id="siteUrl" placeholder="Адрес сайта (https://...)">
            <input type="text" id="apiKey" placeholder="API-ключ">
            <input type="number" id="balance" placeholder="Остаток денег ($)" step="0.01">
            
            <div class="modal-actions">
                <button class="btn-secondary" onclick="closeAddModal()">Отмена</button>
                <button class="btn-primary" onclick="addProvider()">Сохранить</button>
            </div>
        </div>
    </div>

    <!-- Модальное окно редактирования -->
    <div id="editModal" class="modal-overlay">
        <div class="modal">
            <h2>✏️ Редактировать</h2>
            <input type="text" id="editProviderName" placeholder="Название провайдера">
            <input type="text" id="editModelName" placeholder="Модель">
            <input type="url" id="editSiteUrl" placeholder="Адрес сайта">
            <input type="text" id="editApiKey" placeholder="API-ключ">
            <input type="number" id="editBalance" placeholder="Остаток денег" step="0.01">
            
            <div class="modal-actions">
                <button class="btn-secondary" onclick="closeEditModal()">Отмена</button>
                <button class="btn-primary" onclick="saveEditProvider()">Сохранить</button>
            </div>
        </div>
    </div>

    <!-- Toast-уведомление -->
    <div id="toast" class="toast">✅ Изменения сохранены</div>

    <script>
        // ========== ДАННЫЕ ==========
        let providers = [
            {
                id: 1,
                name: 'OpenAI',
                model: 'GPT-4 Turbo',
                site: 'https://platform.openai.com',
                apiKey: 'sk-proj-...abc123',
                balance: 45.50
            },
            {
                id: 2,
                name: 'Anthropic',
                model: 'Claude 3 Opus',
                site: 'https://console.anthropic.com',
                apiKey: 'sk-ant-...xyz789',
                balance: 120.00
            },
            {
                id: 3,
                name: 'Google AI',
                model: 'Gemini 1.5 Pro',
                site: 'https://makersuite.google.com',
                apiKey: 'AIzaSy...def456',
                balance: 8.75
            },
            {
                id: 4,
                name: 'Mistral AI',
                model: 'Mistral Large',
                site: 'https://console.mistral.ai',
                apiKey: 'mist-...ghi012',
                balance: 0
            }
        ];

        let currentEditId = null;

        // ========== ФУНКЦИИ UI ==========
        function renderProviders(filter = '') {
            const grid = document.getElementById('providerGrid');
            const filtered = providers.filter(p => 
                p.name.toLowerCase().includes(filter.toLowerCase()) ||
                p.model.toLowerCase().includes(filter.toLowerCase())
            );

            if (filtered.length === 0) {
                grid.innerHTML = `
                    <div class="empty-state">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                            <path d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4"/>
                        </svg>
                        <p>Провайдеры не найдены</p>
                        <p style="margin-top: 0.5rem; font-size: 0.9rem;">Добавьте нового провайдера</p>
                    </div>
                `;
                return;
            }

            grid.innerHTML = filtered.map(p => `
                <div class="provider-card">
                    <div class="card-header">
                        <div>
                            <div class="provider-name">${p.name}</div>
                            <div class="provider-model">${p.model}</div>
                        </div>
                        <div style="display: flex; gap: 0.5rem;">
                            <button class="edit-btn" onclick="openEditModal(${p.id})">✎</button>
                            <button class="btn-danger" onclick="deleteProvider(${p.id})">✕</button>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="field-group">
                            <span class="field-label">Сайт</span>
                            <span class="field-value"><a href="${p.site}" target="_blank">${p.site}</a></span>
                        </div>
                        <div class="field-group">
                            <span class="field-label">API-ключ</span>
                            <div class="editable-field">
                                <input type="text" value="${p.apiKey}" 
                                    onchange="updateApiKey(${p.id}, this.value)" />
                            </div>
                        </div>
                        <div class="field-group">
                            <span class="field-label">Баланс</span>
                            <div class="editable-field">
                                <input type="number" value="${p.balance}" step="0.01"
                                    onchange="updateBalance(${p.id}, this.value)" />
                                <span class="balance-display ${